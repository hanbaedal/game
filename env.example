# MongoDB Atlas 연결 문자열
# MongoDB Atlas에서 복사한 연결 문자열을 여기에 입력하세요
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# 환경 설정
NODE_ENV=production

# 포트 설정 (Render에서는 자동으로 설정됨)
PORT=10000

# 카카오톡 SDK 키 (선택사항)
KAKAO_SDK_KEY=your_kakao_sdk_key_here 